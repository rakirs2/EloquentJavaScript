for(let i = 1; i <= 10; i++) {

    // multiply i with number
    const result = i * 5;

    // display the result
    console.log(`${5} * ${i} = ${result}`);
}

for(let i = 1; i <= 10; i++) {

    // multiply i with number
    const result = i * 10;

    // display the result
    console.log(`${10} * ${i} = ${result}`);
}